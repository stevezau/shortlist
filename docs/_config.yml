title: Shortlist
tagline: Per-user Plex recommendations, built from each person's own watch history
description: >-
  Give every user on your Plex server their own recommendations, based on what
  they actually watched. Shortlist builds a private "Picked for You" row on each
  person's Plex home screen — visible only to them. Self-hosted, one Docker
  container, no AI key required.

# Where the site lives. Every page derives its links from these two through
# `relative_url` / `absolute_url`, so moving the site is a change to these two lines
# and nothing else INSIDE docs/. Verify that stays true with:
#
#     grep -rln "stevezau.github.io" docs/
#
# which should only ever match this file and docs/README.md — the latter is excluded
# from the build, is the github.com folder view, and runs no Liquid.
#
# ---------------------------------------------------------------------------
# Moving to a custom domain, e.g. shortlist.dev. Nothing here is bought or set up
# yet; this is the checklist for the day it is, in the order that avoids an outage:
#
#  1. DNS first, before anything in this repo. Apex `shortlist.dev` needs the four
#     GitHub Pages A records (and the AAAA records); `www` needs a CNAME to
#     stevezau.github.io. Wait for them to resolve.
#  2. Add `docs/CNAME` containing exactly `shortlist.dev`, and set the custom domain
#     in the repo's Pages settings. Do NOT add that file before step 1: Pages starts
#     serving the custom domain the moment it exists, and until DNS resolves the site
#     is simply down. Turn on "Enforce HTTPS" once the certificate is issued.
#  3. Only then change the two lines below to `url: https://shortlist.dev` and
#     `baseurl: ""`. Getting this out of order is the careless failure mode: the site
#     serves from the apex while every internal link still points at /shortlist/*,
#     so every page on the site 404s while looking fine in the build.
#  4. Fix the absolute links that live OUTSIDE the templates, because no Liquid
#     rewrites them: two in `docs/README.md`, and four lines in the repo's root
#     `README.md` (three header links, plus the Documentation heading, which carries
#     the address as its link TEXT as well as its href).
#  5. Re-submit the sitemap in Google Search Console for the new property, and keep
#     the old github.io property until the new one is indexed. GitHub 301-redirects
#     the old subpath URLs to the custom domain, so inbound links keep working, but
#     Search Console treats the apex as a different site with no history.
#  6. `docs/robots.txt` starts being read at all. On a project subpath it never was:
#     only `stevezau.github.io/robots.txt` counts there, and this repo cannot write
#     that file. Nothing to change, but that is the concrete SEO win.
#
# Not affected, because they resolve at request time or are already absolute:
# the social-preview card (built from `image` + `absolute_url`), the JSON-LD in
# _includes/head.html, the search index in search.json, and every shields.io badge.
url: https://shortlistapp.dev
baseurl: ""

repo: https://github.com/stevezau/shortlist

# The host shown in the landing page's fake address bars. A `.local` name says
# "this runs on your own network" without a sentence about it, and 5959 is the
# real default port, so it doubles as a hint about where the app turns up.
demo_host: shortlist.local:5959
# NOT named `image` — that key is already taken further down by the social-preview card that
# jekyll-seo-tag reads off each page. Two different meanings under one name would be a trap.
# The Docker Hub namespace doubles the z and is NOT the GitHub username `stevezau`; the
# single-z account was lost, so don't "correct" it. GHCR still gets the identical image.
docker_image: stevezzau/shortlist
docker_image_url: https://hub.docker.com/r/stevezzau/shortlist
ghcr: ghcr.io/stevezau/shortlist

# /getting-started/ rather than /getting-started.html. Worth the change while the
# site is days old and has no inbound links to break.
permalink: pretty

# The card Reddit, Discord, Slack and X render when someone shares a docs link.
# social-preview.png is the real Plex row with the name and pitch composited over
# it, at the 2:1 those platforms actually want — the bare row screenshot is 3:1 and
# gets its top and bottom cropped away. This has to go through `defaults` rather
# than a top-level `image:` — jekyll-seo-tag only reads page.image, never site.image.
defaults:
  - scope:
      path: ""
    values:
      image: /images/social-preview.png
      layout: doc

# Ownership proof for Google Search Console (where the sitemap is submitted).
# Public by design — it's served as a meta tag on every page.
google_site_verification: cqlqedFcoobifs1lLaDIdbBPCPurJ-JlDAIVOTs_02k

# The docs sidebar, in reading order. `nav` drives both the sidebar and the
# prev/next links at the foot of every page, so this list is the single place
# page order is defined.
nav:
  - title: Getting started
    url: /getting-started/
    blurb: Install, wizard, first run
  - title: Guides
    url: /guides/
    blurb: How to do the things people actually want to do
    children:
      - title: The web interface
        url: /guides/interface/
      - title: Rows and templates
        url: /guides/rows/
      - title: What goes in a row
        url: /guides/picks/
      - title: Schedules and runs
        url: /guides/schedules/
      - title: AI and cost
        url: /guides/ai/
      - title: Requests
        url: /guides/requests/
      - title: Troubleshooting
        url: /guides/troubleshooting/
      - title: On the internet
        url: /guides/security/
  - title: Reference
    url: /reference/
    blurb: Settings, API, env vars
  - title: FAQ
    url: /faq/
    blurb: Privacy, Kometa, uninstall
  - title: Plex how-to
    url: /plex-how-to/
    blurb: What Plex itself can and can't do, and the manual methods
    children:
      - title: Improving Plex's recommendations
        url: /improve-plex-recommendations/
      - title: Recommendations from watch history
        url: /plex-recommendations-watch-history/
      - title: A home screen per user
        url: /plex-per-user-home-screen/
      - title: Per-user collections
        url: /plex-per-user-collections/
      - title: Netflix-style rows
        url: /plex-netflix-style-recommendations/
      - title: AI recommendations
        url: /plex-ai-recommendations/
      - title: Tools compared
        url: /plex-recommendation-tools/

# theme: none — the site ships its own layouts. Cayman was the stock GitHub Pages
# theme and looked nothing like the app it documents.
plugins:
  - jekyll-relative-links
  - jekyll-seo-tag
  - jekyll-sitemap

# jekyll-relative-links rewrites the .md links these pages use for each other so
# they work both on github.com and on the built site.
relative_links:
  enabled: true
  collections: false

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor
  # Implementation plans written by the planning skill land in docs/ because that is where
  # the skill puts them, not because they belong on the site. Left in, Jekyll publishes each
  # one as a page and jekyll-sitemap hands it to Google — a 69KB internal spec competing with
  # the pages written for readers.
  - superpowers/
